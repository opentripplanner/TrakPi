{
  trip(
    from: { place: "NSR:StopPlace:553" }
    to: { place: "NSR:StopPlace:337" }
    numTripPatterns: 1
    searchWindow: 1440
    modes: { accessMode: foot egressMode: foot transportModes: [ { transportMode: rail } { transportMode: bus } ] }
  ) {
    tripPatterns {
      expectedStartTime
      expectedEndTime
      duration
      walkDistance
      legs {
        mode
        duration
        distance
        line { publicCode name }
      }
    }
    debugOutput { totalTime }
  }
}
