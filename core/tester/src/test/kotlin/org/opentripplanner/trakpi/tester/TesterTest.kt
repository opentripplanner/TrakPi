package org.opentripplanner.trakpi.tester

import kotlin.test.Test

class TesterTest {
    // Placeholder until Tester has real behavior to assert on. Replace with real tests
    // of the test-execution logic as it is built.
    @Test
    fun `runTest does not throw`() {
        Tester().runTest("A")
    }
}
