{
  trip(
    from: { place: "NSR:StopPlace:269" }
    to: { place: "NSR:StopPlace:288" }
    numTripPatterns: 1
    searchWindow: 1440
    modes: { accessMode: foot egressMode: foot transportModes: [ { transportMode: rail } { transportMode: bus } ] }
  ) {
    tripPatterns {
      expectedStartTime
      expectedEndTime
      duration
      walkDistance
      legs {
        mode
        duration
        distance
        line { publicCode name }
      }
    }
    debugOutput { totalTime }
  }
}
