package org.opentripplanner.trakpi.tester

/**
 * Runs tests against a travel planner that is already started and ready to serve.
 */
class Tester {
    /** Run the test suite against the ready planner identified by [version] and record results. */
    fun runTest(version: String) {
        println("TODO: test $version (assumes the planner is already started and ready)")
    }
}
