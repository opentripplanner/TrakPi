{
  trip(
    from: { place: "NSR:StopPlace:390" }
    to: { place: "NSR:StopPlace:659" }
    numTripPatterns: 5
    searchWindow: 1440
    modes: { accessMode: foot egressMode: foot transportModes: [ { transportMode: rail } { transportMode: bus } ] }
  ) {
    tripPatterns {
      expectedStartTime
      expectedEndTime
      duration
      walkDistance
      legs {
        mode
        duration
        distance
        line { publicCode name }
      }
    }
    debugOutput { totalTime }
  }
}
